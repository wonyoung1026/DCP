#!/usr/bin/bash
# sudo systemctl restart docker
# sudo systemctl enable docker

sudo systemctl restart dcp-web
# sudo systemctl enable dcp-web

sudo systemctl restart dcp-instance-manager
# sudo systemctl enable dcp-instance-manager
